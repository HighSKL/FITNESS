import withAuth from '@/app/Assets/Hocs/withAuth';
import React from 'react';

type PropsType = {}

function WaterPage(props: PropsType) {
    return (
        <div>
            
        </div>
    );
}

export default withAuth(WaterPage);